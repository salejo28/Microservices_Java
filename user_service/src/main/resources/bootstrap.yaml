spring:
  application:
    name: user-service
  jpa:
    properties:
      hibernate:
        globally_quoted_identifiers: true
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
